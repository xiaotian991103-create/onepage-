interface:
  display_name: "ImageGen OnePage"
  short_description: "用纯 ImageGen 生成营销案例 OnePage 成图"
  default_prompt: "Use $imagegen-onepage to audit this case, show me the detailed original visual construction plan and render authorization card, then generate the final OnePage entirely through ImageGen."
